# Notification System Configuration

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Rate Limiting (per hour)
RATE_LIMIT_PUSH=10
RATE_LIMIT_SMS=5
RATE_LIMIT_EMAIL=20

# Simulated failure rate (0.0 ~ 1.0, for retry testing)
FAILURE_RATE=0.1

# Worker
WORKER_POLL_INTERVAL=1
MAX_RETRIES=3
